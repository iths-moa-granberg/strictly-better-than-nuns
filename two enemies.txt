RULES:
Choose one enemy to move, one at a time
When both enemies move complete - sound
Sound and sight is specific for each enemy


---------------------------------------------------------------------------


STRUCTURE:
s: enemy turn
!! c: select currentPlayer
c: selects pace (pace, id)
s: calc stepOptions(id)
s: possible steps(possSteps, stepsLeft)
c: enemy takes step (pos, id)
    s: check target, visible players, path
    s: choose new path (?)
    c: select path (path, id)
c: enemy move complete (id)
s: sound
    s: player select token (heardTo, pID, turn, eID)
    c: player placed token (pos, turn, id(enemy))


C: (on - do - emit)
enemy turn -> selectPace() -> enemy selects pace (pace, eID)
possible steps -> takeStep() -> enemy takes step (pos, eID)
possible steps -> confirmDestination() -> enemy move completed (eID)
choose new path -> playerPlaceToken() -> select path (path, eID)
player select token -> playerPlaceToken() - player placed token (pos, turn, eID)

S:
enemy selects pace -> enemyStepOptions() -> possible steps (posSteps, stepsLeft)
enemy takes step -> enemyStepOptions()
enemy takes step -> chooseNewPath() -> choose new path (paths)
select path -> enemyStepOptions()

enemy move completed -> enemyMoveComplete() -> player select token (heard to, playerId, turn)
player placed token -> endEnemyTurn() -> playersTurn (caught)
enemy move completed -> enemyMoveComplete() + endEnemyTurn() -> players turn (caught)



---------------------------------------------------------------------------


TODO:

a) compatible
b) divide turn
c) seperate sound & sight

flytta enemyPaths index -> serverPlayer

INDEX:
enemyStepOptions()
on enemy selects pace
on select path
on enemy takes step
enemyMoveComplete()
    isSeen()
    makeSound()
endEnemyTurn()


SERVER GAME:
checkEnemyTarget() - wincounter adderas bara en gång om 2 enemies fångar catched player


MAIN:
continutiy if evil vs if !evil


SOUND & SIGHT:
ServerGame: 
addToken()

Index: 
enemyStepOptions()
enemyMoveComplete()
isSeen()
makeSound()

Main + board mottag o rendering